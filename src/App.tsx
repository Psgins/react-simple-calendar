import React, { FC } from 'react';
import CalContianer from './components/CalContainer';

const App: FC = () => {
  return (
    <div className="App">
      <CalContianer />
    </div>
  );
};

export default App;
